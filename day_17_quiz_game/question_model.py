class Question:
    """Initialize the setup for the questions in the game."""
    def __init__(self, text, answer):
        """Initialize the attributes for class question"""
        self.text = text
        self.answer = answer
